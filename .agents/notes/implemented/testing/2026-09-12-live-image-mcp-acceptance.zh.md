# 真实图像 MCP 与多图设计验收待办

Status: implemented
Translation: pending

## 摘要

真实图像 MCP 与多图设计的约定验收范围现已完成。历次正常安装包合计发出 5 次 generate 和 2 次 edit，保留七份真实图片输出；原两次生成回执失败及补充轮的测试脚本失败仍如实保留。新增 2 次 generate 和 1 次 edit 均返回工具结果并被原生 Kimi 读取，分步证据与零图片调用续接共同覆盖选中图片替换、正式保存、PNG/JPEG 导出和重开。此前四图海报、12 图长图的保存、PPTD、版本和独立重开已验证，用户已通过六项人工检查、原 edit 目标与三场景总体旅程。用户随后确认多参考图、mask 组合与付费服务取消行为不进入本期范围，不再补测。结论限于记录的安装包、Agent 组合和本机工作负载，不等同于全平台、语言模型自主审美或公开发布验收。

## 测试连接

| 字段         | 本次测试配置                                                     |
| ------------ | ---------------------------------------------------------------- |
| 供应商名称   | AruHub image2                                                    |
| API base URL | `https://direct.aruhub.com:8443/v1`                              |
| model        | `gpt-image-2.5-sunburst`                                         |
| API key      | 用户已提供；执行测试时通过现有图像连接的机密配置使用，不写入仓库 |
| 官网         | 未提供，不从 API 地址推定                                        |

这是用户提供的测试连接，不是 Folio 产品默认供应商或模型；URL、Key、model 仍由用户配置。协议与支持参数按真实响应核验，不因模型名推断特性。请求必须通过 Folio 内置 image MCP 和已有连接/素材保存链路；直接 HTTP 调通只能作为诊断，不能替代 MCP 验收。

最初新增此待办时仅整理文档，没有执行 API 请求。用户随后恢复整体实施 goal，实际测试使用该连接时，保留显式模型，不自动换供应商或模型，不对失败请求静默重试。使用自建测试提示词和测试图片，不上传用户工作文件；执行记录排除密钥、Authorization、带凭据的 URL 和完整会话转录。

## 必做 TODO

- [x] **真实安装包连接与 MCP 入口。** 固定实际安装包/产品源、操作系统及用户选择的 Agent/runtime/ACP，使用已有设置保存上述连接。记录真实暴露的 image generate/edit 工具和调用路径，确认 model 原样发送、没有产品默认值或模拟响应替代。机密配置缺失时明确记录阻塞，不回退假数据并标通过。
- [x] **真实 generate。** 通过 Folio MCP 生成至少 3 张不同的测试素材，例如产品主体、场景背景和装饰插图；保存返回的真实图片字节，核对 MIME、可解码性、尺寸及素材路径。实际打开图片，不能只看 HTTP 200、返回 URL 或模型的成功描述。该最小样本供后续多图画稿复用，避免每个场景重复生成同一素材。
- [x] **真实 edit。** 使用上述一张真实输出作为输入，通过 Folio MCP 至少完成一次有明确可视变化的编辑，例如保持产品形状和构图、把蓝色主体改成红色。核对传入的原图与请求关联、返回的新素材及原图保留，人工比较是否完成目标且主体可辨；不能以另一张无输入的 generate 冒充 edit。多参考图和 mask 按拟宣称支持的实际组合补测；不支持时记录限制，不静默改协议或放宽结论。
- [x] **生成/编辑 → Agent 读图 → PPTD/Bento。** 使用一个实际支持 Folio MCP 的已配置 Agent 完成调用与真实图片读取，把生成素材用于可编辑画稿；再从选中图片执行编辑并替换目标。确认图片工具本身不提前提交画稿，最终图片引用与确切字节一致，无关元素和旧素材保持正确。Provider 接通、Agent 真正获得图片输入、视觉质量分别记录。其他 Agent 的发现/传输/读图仍归五 Agent 矩阵，不能凭一个 Agent 通过替它们盖章。
- [x] **真实图片负载的保存、恢复与导出。** 至少包含一份 12 个图片元素的多图长图或信息图，以及一份使用真实主体/背景图片的海报。记录独立素材数、原始尺寸及总字节数，至少使用上述 3 张不同真实生成图和编辑输出；复用相同图片时如实记录，不以微型占位图充数。验证裁切、缩放、替换、撤销/重做、自动保存、退出重开、PPTD 往返和 PNG/JPEG 导出，无丢图、错图或失效素材路径。测量加载、编辑、保存、导出与峰值内存，供人工判断目标机器可接受性；不预设未经验证的性能通过阈值。
- [x] **失败与视觉结论归档。** 真实服务出现失败、超时、权限或不支持参数时保留错误事实和已有素材/画稿，不用重复付费请求掩盖失败。故意制造的 HTTP/超时/损坏图/取消等负例优先复用现有模拟设施，不为异常覆盖反复调用真实服务。汇总每次真实调用数量、耗时、响应类型、输出尺寸/摘要及可观测用量/费用，未知费用标未知。人工分别评定生图可用性、编辑目标达成和多图版式/交互体验，不能由自动检查代填。

测试用例的固定输入/断言只约束验收，不写入产品 skill 来强制 Agent 按固定创作步骤工作。图像生成具有不确定性，真实测试不按图片逐像素相等判断成功；技术完整性和视觉判断分别报告。

## 关联实施与完成条件

- [主计划 P6.7](../architecture/2026-09-09-graphic-design-platform.zh.md)承接真实图像服务和多图场景验收，补充原 P6.2/P6.4，不创建新的测试引擎或图像作业系统。
- [T28 安装包矩阵](2026-09-11-installed-five-agent-matrix.md)记录真实服务与具体 Agent/MCP 组合；已有模拟 generate/edit 的通过记录保留，不能更名为真实供应商通过。
- [T29 完整设计验收](2026-09-11-complete-design-acceptance.md)承接真实多图画稿、保存/导出及人工视觉/编辑结论。既有少量小图的性能测量不覆盖这一负载。
- Git 版本功能交付后，将本次真实素材样稿用于 P6.6 历史恢复与素材留存验证；真实 generate/edit 基础验收不等待 Git 版本功能上线。

只有真实 generate、真实 edit、素材进入设计后的保存/重开/导出和人工检查各自有结果，才可关闭这一待办。某项供应商不支持、MCP 未暴露、调用失败或人工未评定时保持该项未完成；模拟测试或打包成功不能替代。尚未执行的状态保持未勾选，不宣称全部 Agent 或全部平台已通过。

最初文档变更未运行真实模型、图像服务、产品测试或构建；以下记录后续执行，文档验证本身不构成图像服务验收。

## 首轮实际执行（2026-09-12，部分结果）

正常 macOS arm64 私有安装包来自 `50ddd41bfb3e88537c0dcbec40c0ce86ca40b8e4`，
构建、DMG 校验及安装身份已验证。通过原生 Kimi 和 Folio 内置 MCP、已有设置保存
用户指定连接；语言模型使用隔离合成服务，图像请求直接发送到用户指定 AruHub。
先行合成轮 `folio-t28-kimi-input-AfwzY3` 完成三次 generate、一次 edit 和四次
原生读图（handle 67079 exit 0），不把此轮计为真实图片验收。

真实轮 `folio-t28-kimi-input-1tmh3w`（handle 85875 exit 1）已有以下结果：

- 第一张通过实际 generate 返回，PNG 为 1024×1536、2,259,449 字节，约 36.1 秒；
  SHA256 `17c17e844c705badf07c42dad425c4f16d0ee283587866c7ce0c3573049d10f6`。
  紧随其后的原生 `ReadMediaFile` 将相同图片字节送入模型请求。已实际打开图片，
  可见绿叶与浅色纸面留白；这一观察不代替用户的视觉质量评定。
- 第二次 generate 已经原生单次批准，随后探针退出。约 27 秒后同会话目录留下
  第二张 PNG，1,460,148 字节，SHA256
  `9fdd23bae6bcda97594913d29275bdcf4b03f777589b826f072c9f3a39115e2d`。
  该文件已复制到同轮 evidence 的 `recovered-asset-2.png`；它没有被探针接收为
  对应工具结果，也没有该轮原生读图证据，只记为恢复产物，不能声称第二项完整通过。
- 第三次 generate 和 edit 未执行。没有自动重试、切换模型或重新生成已有素材；
  后续优先复用已保存图片，先以合成服务查明探针异常再继续未执行操作。
- 关闭桌面时一个所属 MCP 进程暂未退出，导致 harness 保留隔离数据；该进程随后
  自行退出。该事实及失败保留，不把未清理的进程视为成功关闭。

证据保存在系统临时目录下上述轮次的 `evidence/`：第一项工具结果、对应原生读图
证明、图片摘要和 `failure.json`。日志位于私有安装轮 `folio-current-50ddd41b-78c0b9vz`。
Key 只写入隔离设置，trace 在输入前丢弃，文本诊断脱敏，不把含 Key 的配置或完整
会话转录写入仓库。首轮错误消息脱敏时丢失了具体原因，尚不归因为供应商或产品错误。
实际 HTTP 状态和费用没有被独立捕获，费用保持未知。

合成诊断轮 `folio-t28-kimi-input-kkq4pz`（handle 49188 exit 0）复用真实大图作为
合成响应，明确持有第二次图像响应 41.1 秒，原控制流程未复现错误；四次工具和读图
均完成，所属进程与端点清理通过。此结果不能解释原失败，也不构成真实供应商通过。

## 后续生成、超时与真实编辑（2026-09-12）

继续复用上述两张图片，没有重新生成。第三次真实 generate 位于
`folio-t28-kimi-input-CmIQPg`（handle 43037 exit 1）：05:07:42Z 获批后，
探针的全局权限处理器重复进入已经消费的批准卡，等待消失的标题而失败。这是探针故障；
修正为按会话及工具调用逐项处理权限，不扩大许可范围。失败清理继续等待已批准请求，
没有再次发起图像调用，保留了另一个独立的产品故障：Kimi 在 60 秒返回
`MCP error -32001: Request timed out`，而服务请求继续至约 74 秒并保存了真实 PNG。
第三张为 1024×1536、3,283,105 字节，SHA256
`60ca177f79b22e782c4baf5b9eb542eecd26999f9e83a2be798dfb98ade30aea`，
保存在该轮 `evidence/recovered-asset-3.png`。恢复字节不把失败回执改为成功。

Folio 图像 HTTP 原有上限为 180 秒，Kimi 和 Pi MCP SDK 默认仅等 60 秒。
[客户端期限修复](../../implemented/bug-fix/2026-09-12-image-mcp-client-deadline.md)
让 Pi 图像调用等待 210 秒，Kimi 设计会话通过公开配置补充默认值，尊重用户配置；
没有改托管 runtime、加付费重试或扩大服务期限。源码检查已通过，新安装包默认值回归另行验收。

最后一轮 `folio-t28-kimi-input-uO0aN7`（handle 68769 exit 0）仍使用
`50ddd41` 正常安装包，在隔离的 Agent 设置中显式配置公开
`KIMI_MCP_TOOL_TIMEOUT_MS=210000`。该轮先复用并实际读取三张已有图片，
仅新发起一次 edit：以第一张绿叶图为输入，要求保留构图与文字留白，把叶片改为铜色和琥珀色。
47.7 秒返回 1024×1536、2,146,871 字节 PNG，SHA256
`3cc4942c6f551cf7eaf93edff1a99afb42fe7f7e5282545abd5968c54a36057f`。
已实际打开编辑图，观察到原构图和白花保留、叶片变为铜色；这只是技术验收的观察，
用户对质量和目标达成的判定仍待进行。没有把无输入的 generate 计为 edit。

最终 `evidence/assets-manifest.json` 记录四图确切摘要、编辑输入关联和四次原生读图证据；
图片为 `asset-1.png` 至 `asset-4.png`。原生 `ReadMediaFile` 的图片字节进入隔离合成语言
模型请求，因此证明工具和读图链路，不证明真实语言模型自主创作或语义审美能力。
第二、三张此轮读图成功，与各自原回合工具回执未完成是不同结论。
这一轮端点、所属进程和隔离数据清理通过；此前保留的含连接密钥测试配置在确认进程退出后清除。

累计真实请求为 **3 generate + 1 edit，无重试**。HTTP 状态、服务用量和费用没有独立捕获，
仍标未知。四张素材交给完整设计验收复用，不为填补历史回执再付费生成。
多图原生编辑、自动保存、PPTD、导出、Git 素材恢复以及用户视觉评价未完成，以上 TODO 不整体勾选。

## 修正轮导出验证（2026-09-12，新构建 47c0808）

50ddd 包的逐格式真相：诊断 12327 对同一海报画稿交替导出 PNG/JPEG 各三次，
导出 0、1、4（角像素 `[242,246,247]`）正确，导出 2、3、5（角像素 `[44,28,16]`）
为启动画面错帧；同期 DOM 六次全部正确（单个 `.bento-slide`、正确作品文本、无
`#bento-splash`），故障在 `capturePage` 拿到过期合成帧，不在元素选择或编码。
根因与最小修复见 [导出捕获验证](../../implemented/bug-fix/2026-09-12-export-capture-frame-verification.md)；
attach/leave 竞态修复见 [离开等待加载](../../implemented/bug-fix/2026-09-12-leave-attach-loading-race.md)。
两个修复与 T28 取消修复（`89d9b07` → `16a5940`）同入新构建
`47c0808d354ce2227e4fed288a748f4fec9e2435`（DMG SHA256
`f50900870ca1442feee70f6f391e633e6ef330420935ec8a1875a09e11300a62`、asar SHA256
`6925f94faae19bfc002973a50cf5c527b05a42363a990e7a7d2ebe6a5781ce0a`，身份文件见
`folio-current-47c0808d-6nzcp7xj/package-identity.json`）。

新包安装态验收（串行，复用上述四图字节，无新增付费调用）：

- 双格式导出：同一海报新会话导入 → 快照一致 → 保存 → 回读一致 → 交替 PNG/JPEG
  六次，角像素六次全部正确；两种格式各抽一文件读图，均为完整正确海报
  （标题、四张真实图、日期），且同格式重复导出字节一致。证据
  `/tmp/folio-export-repro-oowgDi/evidence`（含 `result.json` 与六个导出文件）。
  旧包逐格式失败保留为历史，不覆盖。
- 可编辑状态检查：同上流程覆盖新会话导入 → 快照 → 保存 → 回读一致，证明本进程内
  导入的 Bento 快照可保存并从存储回读；该轮没有退出应用、启动新进程或复制 profile，
  因此不构成可编辑副本重开验证。新进程重开仍待执行。
- 合成 Pi 取消：`native-image-cancel-passed`，被 hold 的合成图像 HTTP 在真实 UI
  Stop 后、fixture 清理前关闭（旧包失败为清理时才关闭）。证据
  `/tmp/folio-t28-kimi-input-EeOUGj/evidence`。无付费调用。
- leave 回归：新会话 attach 周期内 `design.leave` 三次全部 true 且零未保存对话框，
  但三轮 `exercised:false`，只证明常规 ready 实例的 leave guard 未回归，没有再次命中
  原始 loading attach 的短竞态窗口。结构性修复证据仍由原失败记录与确定性 core 测试承担。
  证据 `/tmp/folio-leave-repro-nByRB0/evidence`。
- Grok 旧探针在 47c0808 与 50ddd 包均未进入 provider 请求，但这不能排除产品回归：
  后续静态核验发现旧隔离守卫会拒绝安装包工作目录下的能力探测，且脚本提前关闭了
  尚未完成验证的配置对话框。修正夹具后的 47c0808 独立轮次
  `folio-installed-grok-stop-v2-S6FMNQ` 已通过能力探测、原生启动及会话创建，
  随后的 `x.ai/hooks/action` 重载请求返回 `-32601 Method not found`，仍未进入
  provider 请求或 Stop/显式恢复。该轮退出 1，清理通过；适配边界由 T20 继续诊断，
  旧失败记录保留，脚本文件名中的 UNEXECUTED 不作为实际执行状态。

人工视觉/编辑体验与目标机器容忍度仍待用户评定；以上“读图正确”仅为代理观察，
不代替人工结论。本轮无新增真实 generate/edit，累计仍为 3 generate + 1 edit。
本段当时的信息图/长图与新进程重开缺口由下节补证；人工结论仍未完成，
本 TODO 不整体勾选。

## 多图与独立重开补证（2026-09-12，正常包 567741b）

正常安装包源为 `567741bde4b0459e33711687ab438aaae70caafb`（DMG SHA256
`d46c66a26c99d45857ed8238205698ea5e5d1558b9c7c77fdc828ecb68efd7aa`、asar
SHA256 `033586c1079167f3f752bdfb6c0fc0046b80e03c00e6d1405c53ff9a2ef5ebdd`）。
本轮只复用已留存的三张 generate 和一张 edit 图片，没有新增模型或付费请求。
完整结果在 `/tmp/folio-t29-real-images-nPfTcw/evidence/result.json`：

- 四图海报完成裁切、缩放、替换、撤销/重做、九次自动保存与 PPTD 往返、四个版本，
  PNG/JPEG 均为 1200×1800；12 图长图完成保存、PPTD 往返、版本，PNG/JPEG 均为
  1200×2400。两个作品在同 profile 应用重启后都与最终 BentoDoc 完全一致。
- hard reload 后切换第二个 Session 的实际轮次命中了旧作品 attach 在途条件；精确
  `data-sidebar-session-id` 行到达目标 URL，attach 完成后 leave 正常返回，未出现原来的
  native 未保存对话框。该边界和确定性测试见
  [离开等待加载](../../implemented/bug-fix/2026-09-12-leave-attach-loading-race.md)。
- 首轮 owned process、端点和临时 profile 清理通过，并导出新的保留副本
  `/Users/macmini/FolioReview-567741b-20260912`。独立新进程随后从该复制 profile 打开
  两个作品，核对 source identity、ready/editable 状态、最终快照、保存回读、PPTD
  素材和版本历史，严格清理自身进程与端点；证据
  `/Users/macmini/copied-profile-verification-akD0AA/result.json`。

运行时保留了旧用户应用与历史 Electron 实例，因此耗时只作为当次观察，不是隔离性能
基准，也没有得到目标机器可接受阈值。Agent 的图片检查不能代替用户对生图可用性、
编辑目标、版式质量和真实编辑体验的判断；这些字段继续保持 pending，整个 TODO 仍未完成。


## 人工检查结论补充（2026-09-12）

用户在 567741b 检查副本的 `开始验收.md` 中确认六项均可接受：四张素材内容与清晰度、
海报视觉、12 图片节点长图视觉、裁切/缩放/文字/撤销重做、自动保存/版本/重开，
以及 PNG/JPEG 视觉结果。对应的人工检查不再待定；原自动化结果中的 pending 字段
保持历史原样，详见[完整验收补充](2026-09-11-complete-design-acceptance.md#human-review-supplement-2026-09-12-567741b)。

本次两份作品由测试脚本直接创建并导入素材，没有 Agent 创作对话，所以其聊天区域
没有历史消息是样本本身的状态，不是已发现的聊天记录丢失。此前说明遗漏了这一点。
六项结论当时没有单独回答 edit 是否达成指定变化，后续独立确认见下节。内存/耗时
沿用本机实测和工作负载范围，不增加通用数值门槛；原两次 generate 回执失败仍然
保留，不能据此将整个真实 MCP TODO 标为全部通过。


### 编辑目标人工确认

用户随后对照原图 `17c17e…10f6` 与 edit 输出 `3cc494…057f`，确认保留构图、
文字留白和真实光照、将主要绿叶改为铜色与琥珀色、不添加文字或水印的目标已达到，
结果可接受。这补齐上述单图真实 edit 的人工判断，不扩大为多参考图或 mask 验收。
本地对比材料位于 `/Users/macmini/FolioReview-567741b-20260912-followup/编辑目标对比.md`；
未新增付费调用。真实安装包入口、单图 edit 与真实多图负载三项现已勾选；生成失败
回执和 Agent 选中图片后调用 edit 并替换目标的完整路径仍按各自证据保留。


## 补充调用授权（2026-09-12）

用户确认三场景总体通过，并明确授权新增的真实生成与编辑测试。本轮计划
2 次 generate + 1 次 edit；失败保留事实与已有输出，不自动付费重试。
用户同时授予后续明确任务内合计不超过 5 次图片调用的常设授权；generate 与 edit
合并计数，拆分执行轮次不重置预算。执行约束见根 AGENTS.md，不重复请求已授权
额度内的审批。调用完成后补记实际次数与结果；授权本身不是成功证据。


## 两次补充生成与保留失败（2026-09-12，v9）

正常 `70c9932` 安装包通过原生 Kimi 和真实 AruHub image MCP 完成两次新增生成，
图片输出均成功；语言 provider 为本地合成测试服务。加上最初第一张有完整工具回执
的生成图，现有三次成功生成证据；最初两次不完整回执仍按失败保留，不被改写。

| 输出 | 技术结果 | 图片摘要 |
| --- | --- | --- |
| 水循环 | 原生读图、仅替换 real-image-1、render 后读图、正式提交均通过；revision 从 d6f11bd8… 到 a7bd3023… | PNG，1536×1024，1,489,911 bytes；SHA-256 `4e191ac121818c8acf42c6a2b177822bf3210e0ad425a2f0573a35cdbf8cd142` |
| 海岸 | PNG 输出、原生读图、写入所选元素、render 后读图均通过；原轮未取得可归档的正式回执断言 | PNG，1024×1536；SHA-256 `22aa979ca852ce8c0b96731d12127283616d1eb96859a7010c825ee862662862` |

证据保留在 `/private/tmp/folio-t29-real-agent-zRhn6V/evidence`。v9 在等待第二个
完成标记在聊天 DOM 可见时超过 240 秒，整体 exit 1；第二项原生工具链已完成，
但不能据此补写其正式保存回执。后续复用这两张真实图片补足所需验证，不重新生成。
本轮实际发出图片工具请求为 generate 2、edit 0，成功图片输出 2；费用无账单依据，
保持未知。root 已打开两张 PNG 检查可见内容，属于代理观察，不代填用户视觉结论。

此前 v3–v7 的初始化、导航、控件定位、输入换行等测试脚本失败均为零图片请求，
保留各轮事实。v8 零收费演练已用正确的完整提示词、稳定元素引用和四次原生 Read
到达图片调用前边界；它的旧终端摘要误把一个演练 operation 计为 imageCalls，
实际账本只有四次 Read。v9 已改为从图片工具账本计算请求数，并在失败时保留账本。


## 保留输出续接与第三次补充调用（2026-09-12，v11）

同一 `70c9932` 正常包的独立续接轮先复用海岸 PNG，通过原生 ReadMediaFile、
Write、render 与读图取得正式提交回执，revision 为
`74c34fdbad28d3d59ecf80038a9a26bf45747ec7b2940b20bc5fe271206a3a65`。
仅 real-image-2 的图片引用改变，原始素材保持；本段没有再次 generate。
该成功续接不把 v9 原轮的回执等待失败改为通过。

随后从选中的 real-image-0 发出本轮唯一一次真实 edit，输入为原植物图
`17c17e…10f6`。工具返回真实 PNG，并完成原生 ReadMediaFile：1024×1536，
SHA-256 `f043e6358c122e3ca3c224c281766e6dd51efb5fb9e85338c3858fab8b1771fa`。
root 打开图片观察到铜色/琥珀色叶片、浅色留白与白花，没有添加文字；这是代理观察，
既有人工 edit 目标通过结论仍只对应其实际检查的旧输出。

本轮测试脚本把 Write 的权限工具 ID 前缀写死为 `0:`，第二个操作实际为 `1:`，
因此 edit 后的 Write 未获匹配处理，240 秒后 exit 1。证据保留在
`/private/tmp/folio-t29-real-agent-ZkBQEK/evidence`；这轮只证明真实 edit 输出和
读图，未证明该 edit 的正式保存。修正测试匹配为所属会话内精确 pending ID，
后续只复用现有 PNG 续接保存，不再发起图片调用。

至此新增实际图片请求合计 **2 generate + 1 edit，三个输出成功，无付费重试**；
加上首轮历史累计为 **5 generate + 2 edit**。v9、v11 的整体失败与具体已通过步骤
分别保留；没有供应商账单或计费回执，实际收费与费用未知。两个运行的进程、端点
及隔离 profile 清理通过，临时机密文件已删除。


### 零图片调用的保存与导出（v12）

`folio-t29-real-agent-kRWv3g` 复用编辑 PNG，原生读图、只替换 real-image-0、
render 后读图及正式保存均完成；revision 为
`17e60bd70178f1fd8ada112d01e86c9a99dc551be6b0d3aeb28a7d64c2643a86`。
原素材摘要仍为 `17c17e…10f6`，两张已生成图片作为明确的测试基线保留。
本轮发出零次 image MCP 请求。PNG/JPEG 均导出为 1200×1800，root 已分别打开，
可见完整标题、四图、底部日期，未出现此前的启动画面错帧。

整轮随后在重启定位旧会话标题时失败：原生标题生成已改名，而测试仍等待旧标题。
该 exit 1 和已成功步骤保留在该轮 `evidence/result.json`，不声称重开已通过。
后续免费验证按稳定 session ID 定位，继续核对真实快照与素材。


### 最终免费续接通过（v13）

`/private/tmp/folio-t29-real-agent-Jpf4ce/evidence/result.json` 记录整轮 exit 0、
status passed、reopened true、cleanup passed。正常安装包仍为 `70c9932`；
恢复源为上述同一份真实编辑 PNG，不提供机密配置、不调用 generate/edit。
原生读图 → 仅替换所选元素 → render 并读图 → 正式提交均通过，revision 为
`28e3765571e4cc96c219e3e40a4ceeff2fff2383287bbd8350c4e9beee6604e2`。
原始图片字节保留，无关元素不变。PNG/JPEG 均为 1200×1800；重启后按稳定会话 ID
打开，canonical 与 Bento 快照均匹配。root 检视导出画面，可见完整作品。

本补充批次图片请求数仍为 **3（generate 2、edit 1）**，没有付费重试；v12/v13
均为零图片请求。最终成功不覆盖 v9/v11/v12 的原轮失败，也不将免费文件续接伪装为
另一次真实 MCP 调用。结合既有人工结论和已记录的本机负载结果，本 TODO 及 T28/T29
共享的真实图像要求完成，无需重复人工审批或个人 Kimi 插件安装；不据此推断多参考图、
mask、所有平台或付费服务取消行为。公开发布未执行。


### 收尾检查边界

文档归档首轮 `pnpm check` 因两个 Claude 本地凭据用例继承当前终端的认证环境而失败，
记录在 `/tmp/folio-final-followup-check.log`，不标通过。只在测试子进程去除
`ANTHROPIC_*` / `CLAUDE_CODE_*` 后，同一认证文件 31 项通过；用户配置没有修改。
完整隔离环境 `pnpm check` 随后 exit 0，记录在
`/tmp/folio-final-followup-check-clean.log`。`pnpm run docs check` 与格式检查通过；此处保留
显式 `run`，避免误用打开 npm 包主页的内置命令。


## 范围收尾决定（2026-09-12）

用户确认本期真实图像验收不再扩展：多参考图与 mask 组合、付费服务取消行为
（含真实 HTTP 取消语义）不进入本期范围，不为此发起补充 generate/edit 调用；
既有 5 generate + 2 edit 即为最终真实调用账目。原两次 generate 回执失败、
v9/v11/v12 各轮脚本失败及合成取消回归的边界按原样保留，不改写为通过，也不
据此重测。模型自主创作质量与语义审美仍不做结论；性能结论保持本机负载建议，
不设通用阈值。未来若验收上述组合，需另行授权并新建验收记录。
