import type { SVGProps } from 'react';
import { Worktree } from '@/ui/icons';

export function WorktreeIcon(props: SVGProps<SVGSVGElement>) {
  return <Worktree width="1em" height="1em" aria-hidden="true" {...props} />;
}
