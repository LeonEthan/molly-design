# Generative layered design: task and plan

Status: draft
Previous approval: [2026-09-25 owner approval](../.agents/notes/proposed/feature/2026-09-24-generative-layered-design-workflow.md#spec-approval-2026-09-25)
Translation: current

[中文](generative-layered-design-workflow.zh.md)

## Goal

Given a brief or reference image, Molly's embedded Agent completes the design process from intent understanding to final polish, delivering a layered artwork that users can edit, save, reopen and export. **Make the end-to-end path work first; optimize turn length, tokens and cost afterwards.** The core deliverable is a complete, usable skill solution. This round targets OpenAI Image 2.5 through the user's configured connection and explicit model; compatibility with other image models follows later without restricting the connection settings now.

This revision updates the previously approved plan: start with the user's design thinking, inspiration and references, and actively research design sites when those inputs are absent or insufficient for a design decision. The initial implementation has landed. Acceptance run 1 failed; run 2 has ended, with independent human editing, save/reopen/export and visual acceptance still unverified. The linked note records the implementation and run evidence; these runs do not validate this revised guidance.

## Core workflow

1. **Intent understanding:** understand the design purpose and the user's design thinking; organize content, copy, materials and supplied references.
2. **Inspiration and research:** prioritize the user's inspiration and references. When they are absent or insufficient to support design decisions, actively research design sites such as Pinterest to fill those gaps while preserving the user's direction. Sufficient supplied material can be used directly.
3. **Creative generation:** write prompts, generate several full-image composition drafts, and let the Agent select the best.
4. **Layer decomposition:** use the selected draft as a reference to regenerate complete elements individually with the image model, including occluded portions.
5. **YAML recomposition:** write `design.yaml` to assemble the layers into an editable artwork.
6. **Text handling check:** decide which text becomes editable text and which is better represented as an image or vector.
7. **Holistic review and adjustment:** render and inspect the composition, then regenerate or edit layers that need adjustment.
8. **Consistency check:** compare against supplied materials, copy and intent; correct deviations with the image model or artwork edits.
9. **Final review and polish:** refine the result, deliver the artwork and disclose remaining deviations.

## Design approach and responsibilities

- Put workflow experience, prompting and comparison methods in the existing design Skill. The Agent chooses, repeats or adapts steps; the application adds no stage controller or mandatory creative checkpoints.
- Reference reconstruction also regenerates complete layers; the supplied image can replace draft exploration. Local edits remain focused on the user's request.
- Distinguish delivery assets from design references and choose layer granularity by independent editing value. Leave fidelity, completion, shadow/steam grouping and other open choices to the Agent, without exhaustive case rules or an asset-redraw approval process.
- Teach side-by-side, overlay, local-difference and light/dark-background comparisons that are executable with shipped tools: native render plus image reading, and the Node helper below. Guidance must not depend on Python or other runtimes absent from a user's machine. Human judgment establishes visual quality; pixel scores do not replace it.
- **Tool errors follow Pi's native behavior.** A failed or uncertain image call returns to the Agent as an ordinary tool error, and the Agent decides what to do next. The application adds no run stop, retry fence or retry of its own for tool errors. Restoring a crashed or restarted run still never replays an already-dispatched call; that is session recovery, not tool-error handling.
- **Auto-review permission mode.** Code execution is a core design capability. Pi design sessions offer an auto-review mode that judges risk by effect rather than command form: ordinary shell commands run freely inside an OS sandbox, actions that leave its boundary are judged by a classifier, and only its denials or failures prompt. The default mode keeps today's prompts. Community permission packages were evaluated (see the note).
- Reuse the embedded Agent, image tools, native rendering and save path. BentoDoc remains the single editable truth, with YAML for Agent authoring. Preserve the read-only, commit and version boundaries.

## Implementation tasks

| Task                  | Deliverable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Design experience     | Update the design and image Skills with generation, layer recomposition and review guidance; align reconstruction guidance. Reword the per-turn skill pointer so the Agent reads `graphic-design/SKILL.md` before design work instead of treating it as optional helpers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Image capability      | Add optional `background` (transparent/opaque/auto) and `output_format` (png/jpeg) to generate/edit; reject transparent JPEG before dispatch. Send edits as JSON with ordered source images and an optional mask as data URLs, keeping existing input validation, size limits and asset import; remove the then-unused multipart branch. No default model, allowlist or protocol fallback.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Native tool errors    | Remove the harness conversion of dispatched image failures and unknown MCP delivery into run termination, so both reach Pi as tool errors. Keep crash/restart no-replay protection and asset receipts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Auto-review mode      | Add a Pi permission mode selectable in the existing composer permission control. Shell commands run without prompts inside an OS sandbox from `@anthropic-ai/sandbox-runtime` (exact pinned version), attached through the SDK's `BashOperations` hook in Molly's own bash tool: reads everywhere except credentials and Molly private data, writes only in the session workspace and temp directories, network limited to pre-allowed development and design domains (package registries, GitHub, font and icon services, common CDNs). Risky effects (a connection to a domain not yet allowed, a request to run a command outside the sandbox after a blocked access) go to a classifier, the vendored Pi extension `pi-auto-approval` (exact pinned version, curated like the existing `pi-ask-question`), which judges them against the user's request with the session model; a classifier deny, failure or timeout falls back to the user's prompt. Pi file tools follow the same workspace boundary through the existing approval hook; Molly design tools, local attachment sharing and granted browser sites are approved automatically; the host retains workspace containment and run/connection checks. Record automatic approvals and user decisions as authorization provenance. |
| Optional asset helper | Extend the existing Node image script: inspect alpha (dimensions, counts, bounds for `alpha > 0` and optionally for alpha above a chosen threshold); trim fully transparent outer padding, optionally keeping only the region around pixels above a chosen alpha plus a margin; report crop offsets; build side-by-side, overlay and light/dark comparison sheets from two same-size images. Every processing option is explicit and off by default; source files are never modified. Initially non-interlaced 8-bit RGBA PNG.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Integration           | Align affected rules and Specs (below); verify packaged Skills, tools, permission mode and the desktop path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

Contracts affected: the error-handling and approval rules in `packages/harness-pi/AGENTS.md` and `apps/cli/src/session/AGENTS.md`; image tool rules in `apps/cli/src/mcp/AGENTS.md`; skill-material rules in `packages/design-authoring/AGENTS.md`; the design sentence in root `AGENTS.md`; paid-operation and approval intent in the [embedded Pi harness Spec](molly-embedded-pi-harness.md); image tool and authoring guidance in the [design platform Spec](graphic-design-platform.md). Each changes with its implementation, not ahead of it.

## Acceptance

1. **End-to-end usability run (primary):** after implementation, start one new design through Molly's native interface with the embedded Pi Agent, the configured OpenAI Image 2.5 connection and auto-review mode, giving only a plain brief without stage instructions. The Agent should read the skill, use supplied design thinking and references first, research design sites when those inputs are absent or insufficient, generate and select a draft, generate transparent layers, recompose them in `design.yaml`, review with native renders and save. The user then independently edits a lower layer, saves, reopens and exports. Record which decisions the supplied material supported and which gaps required research, image calls, tool errors and how the Agent handled them, remaining permission prompts, elapsed time and visible deviations. Human judgment decides visual acceptance; a failed run is kept and reported, not relabelled.
2. **Interfaces and assets:** extend the existing MCP→request→asset-import tests with synthetic responses for parameters, source/mask data URLs, alpha and published assets; a synthetic provider error reaches the Agent as a tool error without ending the run. Script tests cover retained pixels, offsets, optional trims, comparison sheets and source preservation. Permission tests cover auto-approved and still-prompted calls. Test observable behavior only.
3. **Evidence limits:** the earlier segmented MVP results support the plan but do not replace end-to-end acceptance. Do not claim general visual reliability from one run. Golden-replica experiments from the earlier phase are not acceptance for this task.

## Out of scope

Turn-length, token and cost optimization; automatic denoising or placement; dedicated comparison UI; classifier review of in-sandbox actions; other third-party permission extensions; new orchestration or storage; WebP and general codec expansion. Bento's fractional-scale crop rendering issue is tracked in [#15](https://github.com/LeonEthan/molly-design/issues/15), and Qwen/Seedream integration in [#16](https://github.com/LeonEthan/molly-design/issues/16); both remain separate follow-ups.

## Open questions

- The initial runs exposed blocked temporary paths and attachment prompts. The next manual run checks the corrected paths and records remaining blocked accesses or prompts; domain and cache policy may still need tuning.
- `@anthropic-ai/sandbox-runtime` is a beta research preview and `pi-auto-approval` is an early single-author release (0.1.1); both are pinned, and upgrades need review.
- A classifier can be influenced by untrusted content the Agent reads (web pages, briefs); classifier approvals are limited to escalations. Preserve its verdicts in run evidence and check remaining escalations in the next manual run.
- With native tool errors, an uncertain image call may be repeated by the Agent and billed twice. Preserve unknown outcomes and record any subsequent Agent calls; the application adds no automatic paid retry.

## Evidence

- [Draft and validation record](../.agents/notes/proposed/feature/2026-09-24-generative-layered-design-workflow.md): segmented real image API, transparent trim, desktop editing and embedded Kimi comparison/correction evidence, with limitations.
- [Design platform contract](graphic-design-platform.md): editing, persistence and Agent lifecycle boundaries.
- The linked validation record separates the pre-implementation inspection from the changes that landed and subsequent acceptance runs. Passing interface tests does not establish end-to-end or visual acceptance.
